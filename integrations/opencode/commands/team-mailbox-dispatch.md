---
description: 整理开发测试与验收派发或回报，预览并经明确授权后发送；不自动接单执行
---

Load the `team-mailbox-dispatch` skill through the skill tool by name, then follow
it for this explicit scenario request. User intent and context: $ARGUMENTS

If the skill is missing, explain that its whole folder (including references/)
must be installed in the same OpenCode scope's skills/ directory and stop.
Do not install it automatically or invent an alternative protocol.
This command is not authorization to send. Clarify assignment/report intent if
ambiguous; ordinary file sharing, investigation updates and chat remain ordinary
messages. Do not poll/claim/execute tasks, configure the host or center, discover
credentials, commit or push. Preview first; send only with explicit authorization.
