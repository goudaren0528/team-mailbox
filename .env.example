# Not loaded automatically. Use process environment or node --env-file=... explicitly.
MSG_HOST=127.0.0.1
MSG_PORT=8787
MSG_DB_PATH=./data/msg.sqlite
MSG_ACCESS_CONFIG=./access.json
# Member bridge: only the server address is required; device remark is optional.
MSG_SERVER_URL=http://127.0.0.1:8787
MSG_DEVICE_NAME=desktop-agent
